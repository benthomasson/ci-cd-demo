---
- name: Local Test Runner Git Hook Rules
  hosts: all
  sources:
    - name: webhook
      webhook:
  rules:
    - name: debug messages
      condition: +payload
      action:
        debug:
    - name: run tests 1
      condition: payload.src_path == "{{src_path}}"
      action:
        run_playbook:
          name: run_pytest.yml
          post_events: true
    - name: print output
      condition: +output
      action:
        print_event:
          glob: output
...
